# Core backend — installs cleanly on Windows without C++ build tools
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic==2.10.3
pydantic-settings==2.6.1
httpx==0.28.1
groq==0.13.1
faker==33.1.0
python-multipart==0.0.20
aiofiles==24.1.0

# Optional (not used by app yet — install separately if needed):
# redis==5.2.1
# alembic==1.14.0
# chromadb==0.5.23   # requires Microsoft C++ Build Tools on Windows
